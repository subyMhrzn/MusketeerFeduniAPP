import 'package:flutter/material.dart';

class HalfpriceScreen extends StatelessWidget {
  const HalfpriceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
